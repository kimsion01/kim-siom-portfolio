# Image Quality Fix Notes

수정 적용 내역

- `오늘, 해달` 원본 JPG를 2x WebP 자산으로 생성해 Retina/고해상도 화면에서 덜 깨지도록 개선했습니다.
- `오늘, 해달` 상세 페이지 CSS에서 와이드 카드가 이미지를 과하게 늘리지 않도록 `max-width`와 `object-fit: contain`을 적용했습니다.
- `보테지앙 패키지` 이미지를 2x WebP 자산으로 교체했습니다.
- `보테지앙 패키지` 섹션은 내부 스크롤 없이 한 번에 보이도록 `packageFitImage` 레이아웃을 추가했습니다.
- `보테지앙` 프로젝트 카드 이미지는 카드 안에서 작고 선명하게 보이도록 `packagePreview` 스타일을 추가했습니다.
- `신상성형외과` 세로 리디자인 이미지는 3x WebP 자산으로 교체해 가로 깨짐을 줄이고, 가로 스크롤 없이 세로 스크롤만 유지하도록 했습니다.
- `오늘, 해달` 페이지의 중복 `<h3>` 태그 1개를 제거했습니다.

참고

- 이번 ZIP 안에 들어 있던 원본 이미지 자체가 낮은 해상도로 저장된 경우가 있어, 완전한 원본급 복원은 불가능합니다.
- 대신 브라우저에서 이미지가 과하게 확대되어 깨져 보이는 문제를 줄이고, 고해상도 WebP 자산으로 교체해 실제 포트폴리오 표시 품질을 개선했습니다.
